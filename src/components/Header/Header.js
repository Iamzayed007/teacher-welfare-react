import React from 'react';
import './Header.css'

const Header = () => {
    return (
        <div className="header-section">
            <h1>Make an Teachers Welfare Society</h1>
            <p><small>If education is the key, then the teacher is the hand that holds the key! How can the key alone open a door? We must always care for teachers!</small></p>
            <h4>Total Budget: 50000 BDT.</h4>
        </div>
    );
};

export default Header;